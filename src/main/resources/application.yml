server:
  port: 8088

# 邮件配置
mail:
  imap:
    server: imap.qq.com
    port: 993
    protocol: imaps
    username:  # 邮箱
    password:  # QQ邮箱的授权码
    attachment-dir: D:\\ # 附件保存目录
    # 连接配置
    connection:
      timeout: 30000 # 连接超时(毫秒)
      read-timeout: 30000 # 读取超时(毫秒)
      write-timeout: 30000 # 写入超时(毫秒)
    # 监控配置
    monitor:
      idle-timeout: 10000 # IDLE命令超时时间(毫秒)
      keep-alive-interval: 300 # 保活间隔(秒)
      reconnect-delay: 15 # 重连延迟(秒)
      short-delay: 5 # 短暂延迟(秒)
      long-delay: 30 # 长延迟(秒)
    # 日志配置
    log:
      debug-enabled: false # 是否启用调试日志

